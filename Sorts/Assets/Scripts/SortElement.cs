public struct SortElement
{
    public int Index;
    public int Step;
    public int Number;

    public SortElement(int index, int step, int number)
    {
        Index = index;
        Step = step;
        Number = number;
    }
}
